name: Workflow Name
on: 
 push:
  branches:
   - main

jobs:
  say_hello:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v0.0.0

      - name: Greeting Message
        run: echo Hello, world
